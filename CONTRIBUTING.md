# Contributing

Agent Workflow Studio demonstrates controlled agent execution with logs and approval gates.

## Workflow

1. Keep workflow steps deterministic.
2. Run `npm test` and `npm run demo`.
3. Document changes to approval or retry behavior.

## Quality Bar

- Human approval gates must be explicit.
- Tool outputs should be logged.
- Retry behavior must be predictable.

